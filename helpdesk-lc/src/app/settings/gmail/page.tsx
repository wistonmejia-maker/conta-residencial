/**
 * Gmail Settings Page
 * Connect/disconnect Gmail accounts for each unit.
 * 
 * Features:
 * - List of units with Gmail connection status
 * - "Connect Gmail" button (triggers OAuth flow)
 * - "Disconnect" button
 * - Test connection button
 * - Configure scan filters (labels, date range)
 */

export default function GmailSettingsPage() {
  return (
    <div>
      <h1>Configuración Gmail</h1>
      <p>TODO: Implement Gmail connection management</p>
    </div>
  )
}
