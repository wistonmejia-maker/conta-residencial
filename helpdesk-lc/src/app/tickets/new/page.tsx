/**
 * New Ticket Page
 * Form for creating a ticket manually (not from Gmail).
 * 
 * Fields:
 * - Unit (select)
 * - Subject
 * - Description
 * - Requester name, email, phone, apartment
 * - Priority (select)
 * - Category (select or auto-classify with AI)
 * - Attachments (optional)
 */

export default function NewTicketPage() {
  return (
    <div>
      <h1>Nuevo Ticket</h1>
      <p>TODO: Implement manual ticket creation form</p>
    </div>
  )
}
