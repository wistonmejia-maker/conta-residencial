/**
 * Drizzle ORM Schema
 * Database tables for Helpdesk LC.
 * Uses Supabase PostgreSQL as the database provider.
 * 
 * Tables:
 * - units: Copropiedades/conjuntos residenciales
 * - gmail_tokens: OAuth tokens for Gmail integration
 * - tickets: PQR tickets from property owners
 * - ticket_messages: Conversation messages within tickets
 * - ticket_tags: Labels/tags for ticket organization
 * 
 * TODO: Install drizzle-orm and drizzle-kit, then run migrations
 */

export {}
